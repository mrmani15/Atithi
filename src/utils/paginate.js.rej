diff a/src/utils/paginate.js b/src/utils/paginate.js	(rejected hunks)
@@ -1 +1 @@
-import _ from 'lodash'
+import _ from "lodash";
@@ -4,3 +4,6 @@ export const paginate = (items, pageNumber, pageSize) => {
-    const startIndex = (pageNumber - 1) * pageSize
-    return _(items).slice(startIndex).take(pageSize).value()
-}
\ No newline at end of file
+  const startIndex = (pageNumber - 1) * pageSize;
+  return _(items)
+    .slice(startIndex)
+    .take(pageSize)
+    .value();
+};
